# Handoff — 2026-07-07: LOD regresje naprawione, ekspozycja, orto-kolor (de-blue), de-light zaparkowany

> Kontynuacja `docs/HANDOFF-2026-07-06.md`. Ten plik = stan wejściowy NOWEJ sesji. Czytaj z
> `docs/TERRAIN-GRAPHICS-CHECKLIST.md` (przed renderem), `docs/TILE-PRODUCTION.md` (przed danymi orto — §3.10–3.15),
> `docs/DELIGHT-RESEARCH.md` (zaparkowany pełny de-light), `docs/FIRN-GENERATION.md` (opis firnu).

## Kontekst repo
- Branch: `feat/atmosphere-effects-toggle`. Bramki przed pushem: `dotnet format MapaTur.slnx --verify-no-changes`
  (⚠️ cykl CRLF — patrz 07-06) + pełne `dotnet test MapaTur.slnx`. NIGDY Claude jako autor/co-author.
- Build desktop: `dotnet build src/MapaTur.App/MapaTur.App.csproj -f net10.0-windows10.0.19041.0`. Ubić
  `MapaTur.App*` przed buildem; `Start-Process` exe; logi `win-x64\logs`.

## 1. LOD — REGRESJE Z 07-05 NAPRAWIONE (kod, testy green; user potwierdził „dużo lepiej")
User zgłosił: „połowa ekranu się nie ładuje / bliska ściana churnuje / reload w kółko". To były **3 realne
regresje z sesji Fable→Opus (07-05)**, NIE „tak ma być" (lekcja: [[take-regression-reports-seriously-not-defensive]]):
1. **Self-kick fill** (`MapPageViewModel.cs` ~3659, `continueFill`): guard `ResidentCount < Desired` był
   defeatowany przez **stale-kafle** zawyżające `ResidentCount` ponad `Desired` po podniesieniu budżetu (93f8046)
   → pętla fill gasła mid-way przy STOJĄCEJ kamerze → połowa bliskiej ściany zostawała na bazie na zawsze.
   Fix: `continueFill = Loaded>0 || Evicted>0 || StalePending>0` (+ ten sam fix `filling`).
2. **Budżet 896→2400 kafli / 6→10 GB** (`MapPageViewModel.cs` ~3924/3943): 896 używał ~1/4 z 16 GB VRAM →
   ciągła eksmisja+reload. Cała piramida = 2.8 GB na dysku, ale jako żywe meshe ~35 GB > 16 GB VRAM, więc cap
   dalej potrzebny; 2400 = lokalny widok nie churnuje. Off-ekranowe kafle nie kosztują draw-calli.
3. **Bąbel oka `EyeBubbleFraction` 0.4→1.0** (`QuadtreeTileSelector.cs` ~212): przy 0.4 look-at dostawał pełny
   ring 2.5 km, a bliska ściana pod okiem tylko 1 km bąbel → detal leciał 5 km dalej, bliska ściana churnowała.
   1.0 = oko ma pełne pierścienie = bliski teren ZAWSZE pełny detal. Test `QuadtreeTileSelectorTests` `/0.4`→`/1.0`.
- **OTWARTE (LOD):** eksmisja WYRZUCA mesh (`resident.Remove`, brak RAM-cache) → re-rezydencja = ponowny odczyt
  `.bdt` z dysku + re-mesh. Przy zwiedzaniu CAŁYCH Tatr (przekroczenie 2400) nadal będzie reload. Fix: **cache
  evictowanych meshy w RAM** (masz 64 GB) → re-rezydencja = upload RAM→VRAM, zero dysku. NIE zrobione (user
  wybrał najpierw kolor).

## 2. Ekspozycja / śnieg (kod) — „wszystko przepalone, za jasno" = podniesiona jasność bazowa
- `TonemapExposure 1.15→1.0` + `sunCol ×1.15→1.0` (`Terrain3DGlRenderer.cs` ~2135/~3226). To był GŁÓWNY powód
  „przepalone bez kolorów" (pakiet C 07-05, `66bcb4a`). ACES zostaje (chroni highlighty), tylko gain=1.0.
- Śnieg (`Terrain3DGlRenderer.cs` ~685): albedo 0.96→0.80, ambient×0.65→0.45, fog na śniegu 0.35→0.65
  (robione pod „śnieg za jasny" ZANIM znaleźliśmy że to ekspozycja). ⚠️ **RE-CHECK: pod ekspozycją 1.0 śnieg
  może być teraz ZA CIEMNY** — ocenić i ewentualnie poluzować albedo/ambient z powrotem.

## 3. ORTO — kolor (DANE w AppData, NIE w git; repro przez skrypty w udokumentowanej kolejności)
**KLUCZOWA DIAGNOZA (obala 07-06):** polskie orto **JEST z GUGiK Ortofotomapy** (overlay 06-10/12), nie Esri.
„Lepione z różnych map / przepalone" pochodziło z (a) NASZYCH §3.4/§3.6 (deprecated — patrz §3.11) + (b) ekspozycji
1.15. Nie z Esri. GUGiK dla Tatr JEST patchworkiem nalotów (różne lata/sezony) — to ograniczenie źródła.

**AKTUALNY STAN ORTO** (8 kafli, `…\Data\dem\tatry-ortho-r{R}-c{C}.png`): baza `.pre-colorfix.bak` (czysty
GUGiK+ZBGIS, głębokie barwy) → **de-blue** → **§3.3 seam-gains** → **§3.12 block harmonize**. Kolejność repro:
1. Restore: `.pre-colorfix.bak` → `.png` (8×).
2. `ortho-deblue-shadow.py` (§3.13): neutralizuje niebieski cast cienia, tint ku **zieleni** (`KG=0.35`, user
   wybrał zieleń jak SK, nie brąz). Per-piksel `B-=0.85·max(0,B-max(R,G))`, `G+=0.35·…`. Zero prania. Czyta pre-colorfix.
3. `ortho-seam-gains.py` (§3.3): inter-cell match (szwy ±3).
4. `ortho-harmonize-gugik-block.py` (§3.12): dopasowanie żółto-zielonego bloku akwizycji (Podhale/Zakopane/
   Czerwone Wierchy) do otoczenia; cross-cell continuous.
- Backupy per kafel: `.pre-colorfix.bak` (KLEJNOT, głęboki GUGiK+ZBGIS), `.pre-blockfix`, `.pre-deblue?`, `.pre-water`,
  `.pre-dehaze`, `.pre-z16patch`. `.pre-colorfix.bak` = jedyny pewny anchor.
- ⚠️ §3.4 `ortho-flatten-exposure` i §3.6 `ortho-patch-dark-acquisitions` = **DEPRECATED** (psuły GUGiK — patrz §3.11). NIE uruchamiać.

## 4. Wypalony CIEŃ w orto — saga (przemyślana do końca)
Problem: statyczny cień kierunkowy nalotu vs nasz dynamiczny sun; do tego niebieski cast (cień oświetlony niebem).
- **De-blue (§3.13)** neutralizuje NIEBIESKI → cień staje się ciemną zielenią (nie znika ciemność, ale nie razi niebieskim).
- **Pełny de-light** (usunąć CIEMNOŚĆ cienia, żeby render robił całe światło) = **ZAPARKOWANE** `docs/DELIGHT-RESEARCH.md`.
  Panel 5 agentów zaprojektował fizyczny algorytm (per-kanał, cast-shadow ray-march, robust sun fit); prototyp
  **NIE ZŁAPAŁ**: DEM 15 m za gruby, orto źle zortorektyfikowane (`corr(luma,N·L)=−0.26`), patchwork nalotów o
  różnym słońcu. Do wznowienia: NCC ko-rejestracja + DEM 1 m + słońce per-segment.
- **Linia szwu akwizycji** (Podhale): TON naprawiony (§3.12+§3.14 `ortho-seam-blend.py`, residual ~0.5/255),
  ZIARNO zmierzone **identyczne** (`ortho-hftex-match.py` §3.15, g=1.00 wszystkie oktawy → skip). Residual =
  chroma/halo/paralaksa sub-progowa = ograniczenie źródła. User: „już tak nie razi". Zaakceptowane.
  (Oba skrypty §3.14/§3.15 są no-op na obecnym stanie — zostają na wypadek re-bake gdyby ton/ziarno wróciły.)

## 5. Inne
- `docs/FIRN-GENERATION.md` — pełny opis techniczny proceduralnych lodowczyków (na prośbę usera).
- README: dodany podpis figury slope-LOD (dwuogniskowy quadtree). ⚠️ **Plik `docs/screenshots/3d-tatry-slope-lod.png`
  NIE ISTNIEJE** — user ma wrzucić zrzut, inaczej obraz się nie renderuje.
- User prosił **niebo = łuk Drogi Mlecznej** (referencje w [[night-sky-milkyway-goal]]) — po orto/renderze.

## Twarde zasady procesu (kosztowały tę sesję — lekcje w pamięci)
- „Działało wcześniej / zepsułeś w refaktorze" = TRAKTUJ SERIO, `git log --since` podejrzanego okna + czytaj kod
  ZANIM powiesz „tak ma być" ([[take-regression-reports-seriously-not-defensive]]).
- Kolor orto oceniaj W APCE (3D), nie top-down ([[ortho-color-judge-in-app-not-topdown]]).
- Nie przepraszaj-i-obiecuj; rób i pokazuj. Fakt od usera = fakt.
- Jedna zmiana → build/bake → apka → werdykt usera. Push/duże ruchy tylko na słowo.
