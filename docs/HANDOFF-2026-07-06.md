# Handoff — 2026-07-06: pakiet światła/cieni, firn/lodowczyki, RAM, telefon; OTWARTE: de-haze orto

> Kontynuacja `docs/HANDOFF-2026-07-05.md`. Ten plik = stan wejściowy NOWEJ sesji. Czytaj razem z
> `docs/TERRAIN-GRAPHICS-CHECKLIST.md` (obowiązkowa przed dotykaniem renderu terenu) i
> `docs/TILE-PRODUCTION.md` (obowiązkowa przed dotykaniem danych orto/DEM — KLUCZOWA dla otwartego zadania).

## Kontekst repo
- Branch: `feat/atmosphere-effects-toggle` — **wszystko zmergowane i na `main`** (HEAD `787fcf4`); branch == main.
- Bramki przed KAŻDYM pushem: `dotnet format MapaTur.slnx --verify-no-changes` + pełne
  `dotnet test MapaTur.slnx` (2026-07-06: **1653 testy green**). NIGDY Claude jako autor/co-author.
- ⚠️ **Cykl CRLF**: `dotnet format` (via .editorconfig) chce CRLF, git normalizuje do LF → po każdym
  checkout `format --verify` zgłasza setki WHITESPACE w `Terrain3DGlRenderer.cs` i paru testach; „naprawa"
  = pusty commit (treść identyczna). Jest spawnowany task na trwały fix (.gitattributes vs .editorconfig).
  Obejście do czasu: `dotnet format` (bez --verify) przed commitem cichnie to.
- Build desktop: `dotnet build src/MapaTur.App/MapaTur.App.csproj -f net10.0-windows10.0.19041.0`
  (ścieżki absolutne). Przed buildem ubić WSZYSTKIE `MapaTur.App*` (czasem zostają 2 instancje → lock DLL:
  `Get-Process | Where-Object { $_.Name -like 'MapaTur*' } | Stop-Process -Force`). Start `Start-Process`,
  logi `win-x64\logs\mapatur-*.log`. Weryfikacja shaderów: `render hitch: setup=` w logu = renderuje;
  `warm-up failed` + `ERROR: 0:NNN` = błąd kompilacji GLSL (stack w logu).
- Telefon: `adb` w `C:\Program Files (x86)\Android\android-sdk\platform-tools`. CI APK od `20deeff` ma
  `versionCode = run_number+200` (było <150 lokalnego → downgrade). Deploy = `gh run download <id>` +
  `adb install -r` (zachowuje dane). Baseline telefonu z tej sesji: **versionCode 293**, user potwierdził
  „działa" (Adreno przełknęło CAŁĄ pracę shaderową 03–06.07).

## Co ZROBIONE i ZWERYFIKOWANE (2026-07-06, wszystko na main)
1. **Pakiet ŚWIATŁO+CIENIE A+B+C+D** (user zatwierdził każdą literę osobno):
   - A `15d9a77`: cienie 1024→**2048 px** desktop (`uShadowTexel` z stałej), PCF 3×3→**Poisson 12 tapów** z
     rotacją IGN, **cross-fade kaskad** (ostatnie 10% zasięgu). shadow ~4 ms.
   - C (osobny commit): **ACES** (Narkowicz) + ekspozycja 1.15 w OBU finalnych passach (composite +
     pass-through). `TonemapStrength=0` = rollback.
   - D `e01e1a4`: cień chmur na `vStableWorldPos.z` + `uCloudShadowOffset` (seed suwaka) — checklista C.8/F.4
     odhaczona.
   - B `0498448`: **`TerrainCurvatureAo`** (promienie METRYCZNE 6/18/45 m, podłoga 0.4, 6 testów) pieczone
     w ALPHA koloru wierzchołka we wspólnym `BuildBlock` (baza+baked+legacy naraz); shader
     `lightSum *= mix(1,vColor.a,uAoStrength=0.6)` PO podłodze anty-czerni. Inwariant C.9 (alpha=AO).
   - cull casterów per kaskada CSM (`e3bda47`): shadow 12→2 ms, sumGpu ~25→6-9 ms.
2. **FIRN / lodowczyki + sezonowy śnieg na realnej rzeźbie** (`60e242c`) — DUŻY, data-first:
   - `FirnSiteData` (9 stanowisk z pułapem wysokości per stanowisko), `MountainStreamData.g.cs` (strumienie
     z OSM przez `testdata/maps/fetch-tatra-streams.py`) rasteryzowane do PUSTEJ dotąd maski wodnej R8
     (zero nowych tekstur; decal wody OFF bo cieki są w orto dla wydajności).
   - `PerennialFirn` (9 testów) **CHANNEL-DOMINANT**: jęzory idą ZA ciekami (strumyczki==rynny==lodowczyki),
     szeroka wklęsłość AO to słaby dodatek N-only → dno kotła nie glazuruje. Shader kształtuje jęzor:
     rozlanie w dół (wachlarz do brzegu stawu, `RunoutDropMeters=550`), szerokość ze wklęsłości, poszarpany
     brzeg (szum tylko dither). Kluczowa lekcja: ~9 iteracji na ZDJĘCIACH usera z werdyktem za każdym razem;
     metryk barwy mylił (haze mleczny=niskonasycony) — trzeba było CZYTAĆ obraz (Read tool na PNG).
   - Sezonowy śnieg (suwak): PORZUCONO losowy szum wiatr/krzywizna → te same realne sygnały (AO+kanały):
     gromadzi w żlebach, zdmuchiwane z grzęd. Spójne z firnem.
3. **RAM orto** (`ce8fa81`): „duplikacja ~5 GB" z 07-02 NIE istnieje (masters współdzielone widok↔renderer);
   pik dekodu 16384×10923 ograniczony MaxDOP=3 (~7 GB→~+1.6 GB); `[Mem]` liczy distinct bufory + working set.
4. **Menu przegrupowane** (`38a3f18`, zaakceptowany projekt): 6 paneli, jeden idiom kontrolek, duplikaty out
   (Warstwy/Teren/Dane; ortho/skały/biomy→Teren; niebo nocne→Pogoda; efekty atmosfery→Ustawienia).
5. **Instalator all-packages** (`579e887`, v1.2): app + DEM + orto + baked z13-16 + cache 1m + mapy +
   hillshade, DiskSpanning (payload ~8.5 GB → Setup.exe + Setup-1.bin 5.1 GB w `installer\out`).
6. **Async start / budżety / anty-wjazd kamery / crash-armor tras / chmury 1500 m** — pełny opis w
   `docs/HANDOFF-2026-07-05.md` (te weszły dzień wcześniej, telefon je potwierdził dziś).

## OTWARTE ZADANIE #1 (priorytet, user w trakcie): DE-HAZE niebieskiego welonu w orto
**Objaw:** kocioł Morskiego Oka / Doliny Pięciu Stawów ma mleczną **niebieskoszarą zasłonę** WYPIECZONĄ w
orto. Dowody (twarde, zweryfikowane): (a) **bez orto znika** (czysty relief hipsometryczny czysty),
(b) **niezależna od naszego cienia** (user przesunął suwak czasu — welon stoi, nasz cień z niego schodzi) →
to DANE orto, nie render. Rejon leży w komórce **`tatry-ortho-r1-c2.png`** (16384×10923, lon 19.95–20.175,
lat 49.10–49.25).

**Decyzje usera (podjęte):** odrzucił opcję 1 (lokalny de-haze skalarem) i przez porównanie wybrał
**wariant A = DE-HAZE obecnych pikseli** (dark-channel prior), NIE opcję B (podmiana na Esri z16 — user
słusznie zauważył, że z16 ma cienie przygaszone do CZERNI + kafelkową kostkę mozaiki; „zamiana szarego na
czarny"). Arkusze porównawcze potwierdziły: z16 czyste ale czarne cienie; de-haze zachowuje detal i nie
psuje cieni.

**GDZIE UTKNĘŁO (twardy wniosek, NIE powtarzać ślepo):** auto-detekcja maski welonu przez cross-reference z
z16 NIE DZIAŁA — komórka `r1-c2` to **patchwork kilku nalotów** (ukośny szew tonalny: górno-prawa część
zielona/jaśniejsza, dolno-lewa niebiesko-szara). `cur_dc − ref_dc` jest zdominowane przez (a) z16-satelita
ma głębsze cienie niż nalot WSZĘDZIE (`bright_excess` median 30 na całej komórce — to różnica ŹRÓDEŁ, nie
haze), (b) tonalne szwy między nalotami. Efekt: maska pali ~70% komórki zamiast welonu. `low_contrast`
gate nie ratuje. Gain z16→current: [0.96, 0.92, **1.32**] (current bardziej niebieski — haze dodaje niebieski).

**STAN PLIKÓW:** prawdziwe orto **NIETKNIĘTE** (wszystko było `--dry`; brak `*.pre-dehaze.bak`, backupy
`pre-colorfix/pre-z16patch/pre-water` na miejscu). WIP `testdata/maps/ortho-dehaze-patch.py` NIEZACOMMITOWANY
(untracked) — punkt startowy, ale detektor zepsuty. Skrypty podglądowe w scratchpadzie sesji
(`ortho-dehaze-samples.py`, `ortho-dehaze-preview.py`, mask-viz).

**REKOMENDOWANA DROGA (następna sesja):** porzucić auto-detektor z16. Dwie opcje odporne na patchwork:
1. **Obrys GEOGRAFICZNY** kotłów (poligon/bbox który user POTWIERDZA) → de-haze (dark-channel prior,
   airlight z welonu ~[174,192,184], transmisja floored `t0≈0.38` żeby cienie NIE w czerń) TYLKO w obrysie,
   piórkowany. Deterministyczne, user-weryfikowalne.
2. Lepszy estymator welonu odporny na szwy nalotów (np. lokalny kontrast + dopasowanie do CZYSTEGO
   sąsiedztwa w tej samej komórce, nie do z16).
**Proces OBOWIĄZKOWY** (§3.9 TILE-PRODUCTION + working agreement): arkusz WARIANTÓW na wycinku prawdziwego
orto → user wybiera → JEDNA komórka `r1-c2` → werdykt w apce → dopiero rollout na inne (jeśli gdzieś indziej
też jest haze). **Seam-safety** (user stracił sesje na szwy): łatać TYLKO wnętrze komórki, twardy
`EDGE_MARGIN` nietkniętych pikseli przy 4 krawędziach → gainy §3.3 zostają byte-identical → po bake
`ortho-analyze-seams.py` MUSI pokazać zero zmian na 10 krawędziach. Backup `*.pre-dehaze.bak`, restart apki,
dopisać jako §3.10 do `TILE-PRODUCTION.md`. z16 cache (13860 kafli) i sampler gotowe w skrypcie.

## OTWARTE (mniejsze, z 07-05, nadal aktualne)
2. **RAM długich sesji**: heap →~16 GB po godzinach: CPU-kopie meshy ~2.7 GB po uploadzie + `MeshBufferPool`
   bez trymowania + Server GC float. Pomiar rozbicia → trymowanie poolu → ewent. zwolnienie CPU-kopii po GL.
3. **Crash planowania tras** (`0xc000027b`): pancerz+hook [FTL] jest (`94b7253`), źródłowy wyjątek NIEZNANY —
   czekać na pierwszy stack w logu, wtedy naprawić przyczynę.
4. **CRLF .gitattributes vs .editorconfig** (spawnowany task) — ujednolicić EOL raz.
5. Strojenie z oka: siła AO (0.6), ekspozycja ACES (1.15), siła firnu — pojedyncze stałe.
6. „Kiedyś": z17/z18 bliskie ujęcia, mikrorelief z16, granit v7, mobilny zestaw orto bez korekcji 3.3-3.6.

## Twarde zasady procesu (kosztowały nas nerwy — nie łamać)
- Jedna zmiana → build → apka OTWARTA → werdykt usera → dalej. Push/merge do main TYLKO na wyraźne słowo.
- **Diagnozuj grafikę WŁASNYMI oczami**: Read tool na zapisanym PNG (arkusz/mask-viz) — metryki skalarne
  mylą (haze mleczny czytał się jako „nie niebieski"). Mierz, nie zgaduj; NAJPIERW pomiar/log, potem hipoteza.
- Fakt podany przez usera JEST faktem — nie weryfikuj ponownie, nie wracaj do obalonych teorii.
- NIE ubijaj procesów nie-swoich (GameBarFTServer ubity „na próbę" = APPCRASH MapaTur `0xc000027b`).
- Zmiany wizualne na danych orto = arkusz wariantów na próbkach → wybór usera → jedna komórka → werdykt →
  rollout. Zero masowego bake bez uzgodnienia wyglądu. Po polsku do usera; kod/commity po angielsku.
